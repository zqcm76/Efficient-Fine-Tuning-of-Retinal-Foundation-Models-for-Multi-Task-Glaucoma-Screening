# 阶段二依赖 (Phase 2)
# 目标环境: Python 3.11.15
# GPU: RTX 3060 12GB (CUDA 12.1)。CPU 也能跑 (smoke_test / 调试)。

torch==2.3.1
torchvision==0.18.1
timm==1.0.9

# 指标 / 数据
numpy>=1.24,<2.0      # 与 torch 2.3.1 配套建议用 numpy 1.x
scipy>=1.10
scikit-learn>=1.2
scikit-image>=0.20
Pillow>=9.5
PyYAML>=6.0

# 说明:
# - 本仓库代码避免任何 Python 3.12-only 语法，可在 3.11.15 运行。
# - GPU 安装 torch 时请按官网选 cu121 轮子:
#     pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
# - RETFound 预训练权重需另行下载 (CFP/视网膜彩照版本)，通过 --retfound 传入。

# 数据增广 / 联合变换 (image + mask + fovea 关键点)，transforms.py 依赖。
# 之前漏列；真实数据集 (MultiTaskManifestDataset) 必需，仅跑 smoke_test 可不装。
albumentations>=1.3,<2.0       # 2.0 有 API 变更，先锁 1.x 与现有 transforms.py 对齐
opencv-python-headless>=4.7    # 提供 cv2，无 GUI 依赖、服务器友好
#   注: 若环境里已装 opencv-python，则二者择一、勿重复安装。
pandas>=1.5                    # check.py / 以 DataFrame 形式传入 manifest 时使用
