# Phase 1 (data pipeline + ResNet34 / UNet baselines)
# Phase 2 will add: timm (RETFound / ViT backbones) + peft (LoRA)
torch>=2.0
torchvision>=0.15
albumentations>=1.3
opencv-python-headless>=4.7
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
PyYAML>=6.0
tqdm>=4.65
Pillow>=9.5
